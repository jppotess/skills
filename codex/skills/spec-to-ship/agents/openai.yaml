interface:
  display_name: "Spec-to-Ship"
  short_description: "Take a spec to verified completion"
  default_prompt: "Use $spec-to-ship to take an issue, task, or spec from kickoff to verified completion with .zenith state and final gap review."

policy:
  allow_implicit_invocation: true
